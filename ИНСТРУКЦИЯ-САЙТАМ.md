# ИНСТРУКЦИЯ САЙТАМ: как работать с GitHub и VPS
# Версия 2 (вечер 25.09.2026) — выдал Молодой Мастер Ибро (старший по серверу), утвердил Бро
# v2: автодеплой-цепь замкнута у ВСЕХ 10 сайтов (публичники роботов на сервере 25.09);
#     добавлена §10 — ловушка терминального санитайзера (урок Ибро, 3 ложные тревоги)

## 0. Карта дома (главное за 30 секунд)

- Сервер: VPS 188.127.227.250 (Smartape), Ubuntu 26.04, вход по SSH — порт **20101** (старый 101 ЗАКРЫТ с 21.09), **только по ключам**, пароли выключены.
- Веб-сервер: **Caddy** (порты 80/443), приложения: **pm2** (порты 3000-3011), база прав root.
- Хозяин всего — **Бро** (единственный арбитр). Старший по серверу — **Ибро**: доступы, файрвол, серты, деплой-роботы, расследования инцидентов.

## 1. Карта портов и доменов

| Сайт (pm2) | Порт | Домен | Автодеплой-робот |
|---|---|---|---|
| mktu | 3000 | мкту.рус | ГОТОВ (Run workflow) |
| iznaki | 3001 | iznaki.ru | ГОТОВ — автопуш on push проверен 25.09 (run стартовал сам) |
| naytea | 3002 | naytea.ru | ГОТОВ (Run workflow) |
| seismos | 3004 | seismos.ru | ГОТОВ (Run workflow) |
| aipat | 3005 | aipat.ru | ГОТОВ — автопуш on push, **ЭТАЛОН, копируй с него** |
| strunino | 3006 | струнино.su | ГОТОВ (Run workflow) |
| rastix | 3007 | rastix.ru | ГОТОВ (Run workflow) |
| ipvsem | 3009 | ipvsem.ru | ГОТОВ (Run workflow) |
| iqin | 3010 | iqin.ru | ГОТОВ (Run workflow) |
| atoros | 3011 | atoros.ru | ГОТОВ (Run workflow) |

С 25.09 цепь деплоя замкнута у ВСЕХ 10: приватник робота = секрет `VPS_SSH_KEY` репо,
публичник робота стоит в authorized_keys сервера (норма 20 ключей, все именованные).
Где пуш-триггер закомментирован — включать ТОЛЬКО после сверки репо = прод (иначе деплой
откатит горячие ремонты, которые делались на сервере напрямую).

Внимание: aipat.ru, ipvsem.ru, струнино.su стоят за турбофларой (CDN) — на origin у них мост-серт, это норма, не чинить.

## 2. Железные правила (нарушать НЕЛЬЗЯ)

1. Не включать пароли SSH (PasswordAuthentication / PermitRootLogin) — вход только по ключам.
2. Не трогать без согласования с Ибро: /etc/ssh/*, ufw, authorized_keys, /etc/caddy/Caddyfile.
3. Не запускать fix-ssh.yml из репо naytea — опасный легаси, включает пароли обратно.
4. Docker / containerd / postfix ЗАПЕЧАТАНЫ (masked) — через контейнеры лез червь. Не размаскировать.
5. Next.js — минимум **16.1.1** (через старые версии лез червь), билд **standalone**, node v22.
6. Ключи/пароли/токены в чат не светить, в git не коммитить. Новые зависимости — смотреть, нет ли postinstall-мусора.
7. Наружу не открывать новые порты. Внутренние службы слушать на 127.0.0.1.
8. В /tmp не класть и не запускать исполняемые файлы — Феникс следит и пишет слепки.

## 3. Как сайту задеплоиться (автодеплой-робот)

Порядок работы робота: `git push` в main → GitHub Actions собирает standalone-билд → заливает по SSH (порт 20101, ключ робота) → атомарная замена папки → healthcheck localhost:ПОРТ → pm2 перезапуск → рапорт в Actions.

Секреты репо (Settings → Secrets and variables → Actions), набор как у aipat:
- `VPS_HOST` = 188.127.227.250
- `VPS_USER` = root
- `VPS_SSH_KEY` = приватный ключ РОБОТА (не личный ключ!)
- `VPS_KNOWN_HOSTS` = строка вида `[188.127.227.250]:20101 ssh-ed25519 AAAA...`
- `REMOTE_DIR` = /var/www/<сайт>
- `APP_PORT` = порт из таблицы
- `PM2_NAME` = имя из таблицы

Проверка успеха: зелёная галка в Actions + https://<домен> отвечает 200.
Если упало: читать лог Actions и чинить СВОЁ приложение. SSH/файрвол/серты не трогать — писать Ибро.

## 4. Как заказать робота (если его нет)

Через Бро к Ибро: «Заведи робота деплоя сайту <имя>».
Ибро сгенерирует ed25519-пару робота, публичник встанет в authorized_keys сервера,
приватник уйдёт в секрет `VPS_SSH_KEY`, deploy.yml создастся по шаблону aipat.
После этого сайту достаточно `git push`.

## 5. Ручной деплой (когда Actions не вариант)

- С 25.09 действующие сайты все с роботами — раздел остался для новых сайтов до заведения робота (§4).
- Штатно для новых: просишь Ибро через Бро — «Обнови <сайт>» — и всё.
- Прямой вход по ключу vps_key_v3 есть только у Бро и Ибро (ключ у Бро в запаске).

## 6. Что поднимается после ПЕРЕЗАГРУЗКИ сервера (проверено 21.09)

Поднимаются сами:
- **ssh** — дверь 20101, только ключи;
- **ufw** — файрвол (20101 = limit, 80/443 открыты, выходы на C2-адреса червя заблокированы);
- **fail2ban** — ловушка брутфорса;
- **canary = ФЕНИКС** — бессмертный охотник на дропперы/руткиты (systemd, Restart=always);
- **caddy** — все сайты на 80/443;
- **clamav-freshclam** — антивирусная база (автостарт включён 21.09);
- **pm2-root** → pm2 resurrect → **все 10 сайтов сами возвращаются** (из /root/.pm2/dump.pm2);
- **cron**: ежедневно в 06:17 snapshot.sh — снимок здоровья + integrity.log.

НЕ поднимаются (запечатаны маской, так задумано): docker, containerd, postfix.
Пароль root по SSH после ребута по-прежнему мёртв. Аварийный путь: VNC-консоль Smartape (пароль в запаске Бро).

## 7. Проверка здоровья (сделай сам, прежде чем звать)

- `pm2 ls` — все ли online; `curl -s http://127.0.0.1:<порт>/` — 200?
- `pm2 logs <имя> --lines 50`; `systemctl status caddy`
- Снаружи: https://<домен>. Для турбофлар-сайтов 502 от edge = вопрос к Ибро, не к приложению.

## 8. Если беда

- Лежит сайт: п.7, чинить ТОЛЬКО своё приложение (`pm2 restart <имя>`).
- Подозрение на взлом (нагрузка, незнакомые процессы): НЕ УБИВАТЬ САМОМУ (урок 21.09: pkill по uid=0 убивает половину системы), звать Ибро — Феникс уже пишет слепки в /root/health/canary.log.
- Потерян доступ: Бро → VNC-консоль Smartape, ключи в запаске (приватный релиз gabbardtools-a11y/vps-backups, тег zapaska-20260921, пароль архива знает только Бро).

## 9. Кто есть кто

- **Бро** — хозяин, приказы только от него.
- **Ибро** — старший по серверу (двери, файрвол, серты, роботы, инциденты).
- **Сайт-чат** — хозяин своего кода: git, билд, своё приложение в pm2. В общедомовые вещи не лезет.

## 10. Ловушка песочницы: терминальный санитайзер ест «[m» (урок Ибро 25.09)

Терминал песочницы вычищает последовательность `[m` (фрагмент ANSI-escapes «сброс цвета») —
даже в обычном тексте файлов. Поэтому YAML-строка `branches: [main]` НА ЭКРАНЕ выглядит
как `branches: ain]`, хотя файл цел. Ибро трижды «ловил» этот фантом 25.09.

Правила:
1. Увидел подозрительно обрезанную/сломанную строку в файле — НЕ «чинь» сразу.
2. Проверь байтами: `python3 -c "print(open('deploy.yml','rb').read())"` — если байты на месте, файл цел, «сломан» только терминал.
3. Правку делать по байтам, а не по отображению: правка по экрану = гарантированная порча рабочего файла.
4. Тот же санитайзер может съедать `[m` в логах, JSON, README — правило одно: экран ≠ байты.
